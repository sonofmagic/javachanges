---
"javachanges": minor
---

Derive snapshot publish versions from pending changesets when a repository is still on a release-qualified version, and support configurable release artifact suffixes such as `-RELEASE`.
