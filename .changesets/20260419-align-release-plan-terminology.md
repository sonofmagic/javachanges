---
"javachanges": patch
---

Align the release plan and status output terminology more closely with official Changesets language.

- Show `Release plan`, `Release type`, and `Affected packages` in `status`.
- Include `packages:` labels in user-facing status, changelog, and generated release-plan PR content.
