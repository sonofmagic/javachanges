---
"javachanges": patch
---

Improve generated release PR Markdown layout.

`plan --apply true` now renders `.changesets/release-plan.md` with a compact summary table and grouped
changeset sections so GitHub and GitLab release PRs are easier to review.
